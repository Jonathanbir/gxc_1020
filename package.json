{
  "devDependencies": {
    "audit-ci": "^7.1.0",
    "eslint": "^8.57.1",
    "eslint-plugin-security": "^3.0.1"
  },
  "scripts": {
    "lint:security": "eslint . --ext .js,.ts",
    "audit:ci": "audit-ci --moderate",
    "test:security": "npm run lint:security && npm run audit:ci"
  }
}
